# 企账通SCRM系统产品需求文档（PRD）

**文档版本**：v1.0
**创建日期**：2026-01-19
**产品名称**：企账通
**目标客户**：代理记账公司
**文档类型**：产品需求文档（PRD）

---

## 目录

1. [产品概述与核心定位](#一产品概述与核心定位)
2. [四端功能设计](#二四端功能设计)
3. [核心业务流程](#三核心业务流程)
4. [自动化规则设计](#四自动化规则设计)
5. [非功能性需求](#五非功能性需求)
6. [MVP功能范围](#六mvp功能范围)
7. [实施建议](#七实施建议)

---

## 一、产品概述与核心定位

### 1.1 产品定位

**企账通**是一款面向**代理记账公司**的客户管理和运营系统，深度整合企业微信，通过**PC后台、H5移动端、企业微信单聊侧边栏、企业微信群聊侧边栏**四端协同，帮助代账公司实现从客户开发、签约、开票、服务到续费的全流程数字化管理。

### 1.2 核心业务

- **客户管理**：客户档案、跟进记录、服务团队、合同收款
- **开票管理**：记录客户开票额度和开票张数，超额计费，阶梯定价
- **团队协同**：业务、财务、外勤配合，群聊流程节点推进
- **自动化运营**：基于事件和规则的跟进提醒、状态流转

### 1.3 核心价值优先级

1. **销售效率**（最高）- 侧边栏快捷操作、快速创建合同收款、一键组建服务团队
2. **客户运营** - 完整客户画像、跟进记录管理、服务团队协同
3. **自动化运营** - 基于事件和日期的跟进提醒、状态流转自动化
4. **数据整合**（基础）- 四端数据统一、报表看板、为运营提供数据支撑

### 1.4 目标用户

- **老板/管理者** - 查看业务数据、配置规则、团队管理
- **业务（销售）** - 开发客户、签单、收款、组建服务团队
- **财务** - 记账报税、开票管理、客户服务、流程节点执行
- **外勤** - 现场收集资料、客户沟通、进度更新

---

## 二、四端功能设计

### 2.1 PC后台（管理配置端）

**核心用户**：老板、管理员、系统配置人员

**定位**：系统配置、权限管理、规则配置（纯配置端）

#### 功能模块

**① RBAC权限系统**
- 用户管理：创建、编辑、禁用用户账号
- 角色管理：预置角色（老板、业务、财务、外勤）+ 自定义角色
- 权限配置：菜单权限、数据权限（按部门、按客户、按个人）
- 组织架构：部门管理、岗位管理

**② 客户设置（卡片式配置）**

每个设置项独立卡片，支持启用/禁用、排序：

- **团队角色**：定义角色名称（业务、财务、外勤、老板等）、角色描述、角色权限
- **预设团队**：配置"角色+人员"组合模板（如：标准服务团队 = 业务张三 + 财务李四 + 外勤王五）
- **产品管理**：
  - 产品名称、价格、开票额度（基础20万/月，超额阶梯定价）
  - 开票张数（套餐包含张数，超额单价）
  - 关联角色（多选，一个产品需要哪些角色配合）
  - **产品流程配置**：
    - **节点流程**：定义一次性执行流程（如：记账流程 = 收集凭证→记账→审核→报税→完成）
      - 流程节点：节点名称、负责角色、节点顺序、节点说明
    - **周期流程**：定义重复性执行流程（如：每月报税提醒）
      - 执行周期：每月/每季度/每年，可设置具体日期
      - 触发动作：发送消息、创建待办、添加跟进记录
- **套餐管理**：
  - 套餐名称、价格、包含的产品（多选）
  - 套餐说明、适用客户类型
- **公司资料**：上传公司介绍、产品手册、案例等资料库，支持分类管理
- **常用语**：系统级常用语（所有人可用）+ 个人常用语（仅自己可见），支持分类、排序
- **禁用语**：设置敏感词库，发送时自动拦截或提醒
- **电子签**：配置电子签模板（合同模板、收款协议），关联第三方电子签服务
- **收款账号**：配置公司收款账号（银行账号、支付宝、微信等），支持多账号

**③ 自动化模块**
- 规则模板：预置常见规则（新客户跟进提醒、合同到期提醒等）
- 自定义规则：基于实体事件（客户添加、合同创建、收款完成等）+ 日期条件（未跟进X天、到期前X天）触发动作
- 动作类型：添加跟进记录、发送消息、创建待办、分配任务等
- 规则执行日志：查看规则执行历史和结果

**④ 客户分配管理**
- 查看所有客户的跟进人
- 手动分配客户（单个/批量）
- 查看客户分配历史（客户ID、原跟进人、新跟进人、分配时间、操作人）
- 权限交接记录

---

### 2.2 H5移动端（全角色通用）

**核心用户**：老板、业务、财务、外勤（所有角色）

**定位**：移动办公、随时查看、快速操作、数据查看

#### 底部Tab导航

**① 首页**（根据角色动态展示）

**老板视角**：
- 欢迎语："张总，早上好！"
- 核心数据卡片：
  - 今日新增客户数
  - 本月合同金额
  - 本月收款金额
  - 待处理事项数
- 待办事项：显示需要审批或关注的事项
- 快捷入口：数据报表、客户列表、团队管理

**业务（销售）视角**：
- 欢迎语："王经理，早上好！"
- 核心数据卡片：
  - 今日跟进客户数
  - 本月新增客户数
  - 本月签单金额
  - 待跟进客户数
- 待办事项：跟进提醒、合同到期提醒
- 快捷入口：我的客户、创建跟进、业绩排行

**财务视角**：
- 欢迎语："李会计，早上好！"
- 核心数据卡片：
  - 今日待记账客户数
  - 本月开票总额
  - 本月超额客户数
  - 待处理流程节点数
- 待办事项：流程节点任务、周期任务提醒
- 快捷入口：我的任务、开票管理、客户列表

**外勤视角**：
- 欢迎语："张外勤，早上好！"
- 核心数据卡片：
  - 今日待走访客户数
  - 本月收集凭证数
  - 待完成节点任务数
- 待办事项：现场任务、节点提醒
- 快捷入口：我的任务、客户地图、打卡签到

---

**② 客户Tab**

**列表展示**：
- 搜索：支持客户名称、联系人、手机号搜索
- 筛选：客户等级、跟进状态、合同状态、时间范围
- 排序：最近跟进、创建时间、合同金额
- 卡片展示：
  - 客户名称、公司名称
  - 客户等级、跟进状态
  - 合同信息、收款状态
  - 服务团队成员头像
  - 最后跟进时间

**客户详情**（点击进入）：
- **基础信息**：客户名称、联系方式、公司信息、开票信息
- **跟进记录**：时间线展示所有跟进记录
- **合同信息**：所有合同列表、收款记录
- **开票记录**：开票额度使用情况（本月/年度）、开票张数统计、超额提醒
- **服务团队**：团队成员及角色
- **快捷操作**：创建跟进记录、查看详情（根据角色权限展示不同操作）

**客户操作**：
- 创建客户
- 创建跟进记录
- **客户分配**（管理员功能）：手动分配客户给其他人

---

**③ 互动Tab**

**核心功能**：
- **创建跟进记录**：选择客户 → 填写跟进内容 → 选择跟进类型（电话、微信、上门等） → 保存
- **客户活动**：展示所有客户相关活动（跟进记录、合同创建、收款记录、节点完成等）
- **我的任务**：展示分配给我的所有任务（流程节点、待办事项、周期任务）
- **任务详情**：点击任务查看详情 → 执行任务 → 填写内容/上传附件 → 完成任务

---

**④ 数据Tab**

**老板视角**：
- 业绩总览：合同金额、收款金额、新增客户数趋势图
- 团队排名：业务员业绩排行、任务完成率排行
- 客户分析：客户等级分布、来源分布、流失预警
- **开票分析**：开票总额、超额客户统计、超额费用统计

**业务（销售）视角**：
- 个人业绩：合同金额、收款金额、新增客户数
- 跟进统计：跟进次数、跟进覆盖率
- 客户分析：客户列表、跟进状态分布

**财务/外勤视角**：
- 任务统计：完成任务数、待完成任务数
- 效率分析：平均完成时长、准时完成率
- **开票统计**：经手开票总额、超额提醒

---

### 2.3 企业微信单聊侧边栏（销售效率核心）

**核心用户**：业务（销售）、财务、外勤

**定位**：**快捷操作、客户画像、工作协同**（销售效率优先的核心体现）

**页面结构**：从上到下分为三个区域

---

**① 客户画像区域**（核心展示，按场景动态切换）

**场景1：新客户开发（未签单客户）**
- **客户信息卡片**：
  - 客户名称、联系人、手机号
  - 客户来源、添加时间、沟通次数
  - 客户等级（潜在/意向/成交）
  - 标签（可自定义，如：制造业、高新技术企业）
- **快捷操作**：
  - 发送公司资料（弹窗选择资料）
  - 发送常用语（弹窗选择常用语）
  - 创建跟进记录
  - 创建合同
  - 设置提醒

**场景2：老客户服务（已签单客户）**
- **客户信息卡片**：
  - 客户名称、公司名称
  - 已购产品/套餐、合同金额
  - 服务团队成员（头像+角色）
  - 合同到期时间、续费提醒
- **服务状态**：
  - 当前服务进度（如：记账进行中）
  - 待处理事项（如：等待客户资料）
  - 最近服务记录
- **快捷操作**：
  - 创建收款记录
  - 续费合同
  - 查看服务团队
  - 进入群聊
  - 创建跟进记录

**场景3：售后问题处理**
- **客户信息卡片**：
  - 客户名称、问题描述
  - 问题等级（紧急/一般/低）
  - 问题提交时间、处理状态
- **跟进记录**：
  - 最新3条跟进记录（时间线展示）
  - 问题处理进度
- **快捷操作**：
  - 添加跟进记录
  - 分配任务给团队
  - 查看完整跟进历史
  - 紧急升级

**客户画像底部**：切换场景按钮 + 查看完整客户信息按钮

---

**② 快捷操作区域**（固定功能入口）

**左侧快捷按钮**：
- 📄 发送公司资料（点击→弹窗选择资料→发送）
- 💬 发送常用语（点击→弹窗选择常用语→发送）
- 📝 创建跟进记录（点击→弹窗填写内容→保存）
- 📄 创建合同（点击→跳转合同创建页）
- 💰 创建收款（点击→跳转收款创建页）
- ⏰ 设置提醒（点击→选择提醒时间→保存）

**右侧动态按钮**（根据场景变化）：
- 新客户：发送报价单、预约上门
- 老客户：查看合同、查看收款、进入群聊
- 售后问题：查看任务、升级问题

---

**③ 服务团队区域**（已签单客户展示）

**团队展示**：
- 服务团队列表（头像+姓名+角色）
- 当前在线状态（在线/离线）
- 点击成员头像→快捷操作：
  - 发送消息（跳转企业微信聊天）
  - 分配任务
  - 查看详情

**群聊入口**：
- 服务群聊卡片
- 点击进入群聊
- 群成员列表（客户+团队成员）

---

**弹窗功能设计**：

**① 发送公司资料弹窗**：
- 资料分类（产品介绍、公司介绍、案例等）
- 搜索资料
- 点击资料→预览→确认发送

**② 发送常用语弹窗**：
- 分类标签（开场话术、产品介绍、跟进话术、节日祝福等）
- 搜索常用语
- 系统级常用语（所有人可见）
- 个人常用语（仅自己可见，可自定义添加）
- 点击常用语→自动填充到聊天输入框

**③ 创建跟进记录弹窗**：
- 选择跟进类型（电话、微信、上门、邮件等）
- 填写跟进内容
- 选择跟进状态（已完成/待跟进）
- 选择下次跟进时间（可选）
- 保存

**④ 创建合同弹窗**：
- 选择产品/套餐（单选）
  - 产品：显示价格、开票额度、开票张数、关联角色
  - 套餐：显示包含的产品、总价
- 选择预设团队（下拉选择，自动填充团队成员）
- 手动调整团队成员（可选）
- 填写合同金额、服务周期
- 预览合同详情
- 提交→创建成功→自动分配服务团队→创建群聊

**⑤ 创建收款弹窗**：
- 选择合同（关联合同）
- 填写收款金额
- 选择收款方式（银行转账、微信、支付宝等）
- 上传收款凭证（图片）
- 提交→更新合同收款状态
- 收款完成后→触发自动化规则（如果配置了）

---

### 2.4 企业微信群聊侧边栏（团队协同核心）

**核心用户**：财务、外勤、业务（服务团队成员）

**定位**：**流程节点推进、任务执行、团队协作**

**核心设计原则**：**只展示与我相关的内容，不展示其他人的任务和流程**

**页面结构**：从上到下分为三个区域

---

**① 我的待办区域**（最核心）

**待办任务列表**：
- 只展示**分配给我**的流程节点任务
- 筛选：待处理/已完成

**任务卡片**（简洁版）：
- 客户名称
- 任务名称：如"收集凭证"、"完成记账"
- 截止时间
- 状态：待处理/进行中/已完成

**任务操作**：
- 点击任务→进入任务详情
- 开始处理
- 完成任务（填写内容→提交）
- 查看详情（查看客户信息、任务说明）

**底部统计**：
- 待处理：X个（红色提醒）
- 进行中：X个
- 已完成：X个

---

**② 我的周期任务区域**

**周期任务列表**：
- 只展示**我负责执行**的周期性任务

**任务卡片**：
- 任务名称：如"每月报税提醒"
- 下次执行时间
- 状态：待执行/已完成

**任务操作**：
- 立即执行（手动触发）
- 查看执行历史

---

**③ 客户快捷信息区域**

**当前群聊对应的客户**：
- 如果群聊关联了客户，显示客户卡片
- 客户名称、公司名称
- 我在此客户服务团队中的角色
- 快捷操作：查看完整客户信息

**如果群聊没有关联客户**：
- 显示群聊基本信息
- 群成员列表
- 群聊创建时间

---

## 三、核心业务流程

### 3.1 新客户开发流程（业务/销售视角）

**流程步骤**：

**Step 1：获得客户线索**
- 客户添加企业微信（通过二维码、推荐、主动添加等）
- 自动同步到SCRM系统
- 触发自动化规则：创建欢迎跟进记录（如果配置）

**Step 2：创建客户档案**
- 方式1：企业微信单聊侧边栏→自动加载客户画像（新客户场景）
- 方式2：H5端→客户Tab→添加客户→填写信息→保存
- 填写信息：
  - 客户名称、联系人、手机号、公司名称
  - 客户来源（渠道）、客户等级（自动评估或手动选择）
  - 客户标签（可选）
- 系统自动分配跟进人（根据客户继承规则）或手动指定

**Step 3：跟进客户**
- 侧边栏快捷操作：
  - 发送公司资料（建立信任）
  - 发送常用语（专业话术）
  - 创建跟进记录（记录沟通内容）
  - 设置提醒（下次跟进时间）
- H5端互动Tab：创建跟进记录
- 自动化跟进提醒：X天未跟进→自动创建待办（如果配置）

**Step 4：创建合同**
- 侧边栏快捷操作：创建合同
- 填写合同信息：
  - 选择产品/套餐（单选）
    - 产品：显示价格、开票额度、开票张数、关联角色
    - 套餐：显示包含的产品、总价
  - 选择预设团队（如：标准服务团队）
    - 自动填充团队成员（业务+财务+外勤）
  - 手动调整团队成员（可选）
  - 服务周期：默认一年
  - 合同金额：自动计算或手动调整
- 提交合同
- 系统自动创建：
  - 合同记录（状态：待收款）
  - 收款待办（分配给业务或财务）
  - 触发自动化规则（如果配置）

**Step 5：收款确认**
- 方式1：侧边栏快捷操作→创建收款→填写收款信息→上传凭证→提交
- 方式2：H5端→客户详情→收款记录→创建收款
- 收款信息：
  - 选择合同
  - 收款金额
  - 收款方式（银行转账、微信、支付宝等）
  - 收款凭证（图片上传）
- 提交后：
  - 更新合同收款状态
  - 更新客户等级（如：从潜在客户→正式客户）
  - **触发关键自动化规则**：
    - 客户画像增加服务团队
    - 自动创建企业微信群聊
    - 邀请客户+所有团队成员加入群聊
    - 发送欢迎消息（如果配置）
    - 创建产品流程的第一个节点任务（如：收集凭证）

**Step 6：服务团队协同**
- 群聊创建成功后：
  - 财务在群侧边栏看到：收集凭证任务（待处理）
  - 外勤在群侧边栏看到：现场走访任务（待处理）
  - 业务在群侧边栏看到：跟进维护任务（待处理）
- 团队成员在群侧边栏执行任务：
  - 开始处理→填写内容→提交完成
  - 流程自动推进到下一节点
  - 下一个节点的负责人收到新任务提醒

**Step 7：持续服务与跟进**
- 财务：执行记账流程节点任务（收集凭证→记账→审核→报税→完成）
- 外勤：协助收集资料、现场沟通
- 业务：定期跟进、客户维护、续费提醒
- 周期任务自动执行：
  - 每月报税提醒→自动发送消息给客户
  - 合同到期前30天→自动创建续费待办

---

### 3.2 老客户服务流程（已签单客户）

**场景A：日常服务**

**Step 1：客户主动联系**
- 客户在企业微信私聊或群聊中提问
- 业务/财务收到消息
- 打开单聊/群侧边栏

**Step 2：快速查看客户画像**
- 侧边栏自动加载客户画像（老客户服务场景）
- 查看关键信息：
  - 已购产品/套餐
  - 服务团队成员
  - 合同状态、收款状态
  - 最近跟进记录

**Step 3：响应客户需求**
- 快捷操作：
  - 发送公司资料（如：产品使用指南）
  - 发送常用语（如：标准回复话术）
  - 创建跟进记录（记录本次沟通）
- 如果涉及服务变更：
  - 创建增购合同（追加产品）
  - 创建续费合同（快到期时）
  - 调整服务团队（更换成员）

**场景B：问题处理**

**Step 1：客户反馈问题**
- 客户在群聊中提出问题
- 相关团队成员收到通知

**Step 2：侧边栏查看问题详情**
- 侧边栏加载客户画像（售后问题场景）
- 查看问题描述、问题等级、提交时间

**Step 3：快速响应**
- 创建跟进记录（记录问题）
- 分配任务给对应角色（如：分配给财务处理开票问题）
- 任务在群侧边栏显示（负责人的待办）

**Step 4：解决问题**
- 负责人在群侧边栏：开始处理→填写解决方案→提交完成
- 系统自动通知客户和团队成员
- 更新跟进记录

---

### 3.3 客户继承/流转流程

**触发场景**：
- 销售人员离职
- 销售人员转岗
- 主管手动调整客户分配
- 自动分配规则（如：按客户数量平衡）

**流程步骤**：

**Step 1：发起客户分配**
- **PC后台**→客户继承管理
- **H5端**→客户Tab→客户分配（管理员功能）
- 选择原跟进人→查看其客户列表
- 选择要分配的客户（支持批量选择）

**Step 2：选择新跟进人**
- 单选或批量选择新跟进人
- 系统检查：
  - 新跟进人的客户数量（避免过度分配）
  - 新跟进人的角色权限（是否可以接收这些客户）

**Step 3：确认分配**
- 预览分配信息：
  - 客户数量
  - 客户列表
  - 原跟进人→新跟进人
- 确认分配

**Step 4：系统自动执行**
- 更新客户表的跟进人字段
- 记录分配历史（客户ID、原跟进人、新跟进人、分配时间、操作人）
- 通知新跟进人（H5端、侧边栏待办提醒）
- 权限交接：
  - 新跟进人可以看到客户的所有历史数据
  - 原跟进人的访问权限被收回（根据权限配置）

**Step 5：新跟进人接手**
- 新跟进人在H5端或侧边栏查看分配的客户
- 系统创建待办："接手客户X，请尽快跟进"
- 跟进记录中自动添加："客户从XXX转交给YYY"

---

### 3.4 开票管理流程

**Step 1：财务创建开票记录**
- H5端→客户详情→开票记录→创建开票
- 或侧边栏→客户画像→开票信息→创建开票
- 填写开票信息：
  - 选择客户、关联合同
  - 填写开票金额、开票张数
  - 选择开票月份
  - 上传开票凭证（可选）

**Step 2：系统自动计算**
- 计算本月开票总额（累计）
- 计算本月开票张数（累计）
- 判断是否超额：
  - 开票额度是否超过20万
  - 开票张数是否超过套餐包含数

**Step 3：超额提醒**（如果超额）
- 自动触发超额提醒规则
- 创建待办任务（分配给财务和跟进人）
- 发送消息："客户XX本月开票已超额，超额金额X元，超额张数X张，应收超额费用X元"

**Step 4：开票记录保存**
- 保存开票记录
- 更新客户开票统计
- 在H5端和侧边栏的客户画像中展示开票信息

**Step 5：开票统计分析**
- PC后台的数据报表（虽然不包含开票管理，但可以在H5查看）
- H5端数据Tab→开票统计：
  - 开票总额、开票张数
  - 超额客户统计
  - 超额费用统计

---

### 3.5 产品流程执行流程

#### 节点流程执行（如：记账流程）

**流程配置**（PC后台）：
- 定义流程节点：收集凭证→记账→审核→报税→完成
- 每个节点配置：
  - 节点名称、节点说明
  - 负责角色（如：收集凭证-外勤、记账-财务）
  - 节点顺序

**流程触发**：
- 收款完成后自动触发
- 创建产品流程的第一个节点任务（如：收集凭证）
- 分配给对应角色的负责人（如：外勤张三）

**节点执行**：
- 负责人在群侧边栏看到待办任务
- 点击任务→开始处理→填写内容→上传附件→提交完成
- 系统自动推进到下一节点
- 创建下一个节点的任务（如：记账任务，分配给财务李四）

**流程完成**：
- 所有节点完成后，流程结束
- 更新客户服务进度
- 记录流程执行历史

---

#### 周期流程执行（如：每月报税提醒）

**流程配置**（PC后台）：
- 定义周期任务名称（如：每月报税提醒）
- 执行周期：每月/每季度/每年
- 执行日期：如每月10号
- 触发动作：
  - 发送企业微信消息给客户（模板可配置）
  - 创建待办任务给对应角色
  - 添加跟进记录
- 关联产品：此周期流程属于哪个产品
- 启用/禁用状态

**流程执行**：
- 系统自动在每月10号触发
- 负责人（如：财务）在群侧边栏看到周期任务
- 点击"立即执行"→选择客户→填写消息模板→发送
- 系统自动发送企业微信消息给客户
- 记录执行历史

---

## 四、自动化规则设计

### 4.1 自动化规则概述

**定位**：通过**配置化的事件驱动规则引擎**，实现客户运营的自动化，减少人工操作，提升销售和服务效率。

**核心架构**：
- 基于 `ruoyi-automation` 模块的通用自动化引擎
- 使用 `@AutoEntity` 和 `@AutoField` 注解自动注册实体和字段
- 通过数据库配置触发器、条件和动作链
- 支持事件驱动、定时任务、Webhook等多种触发方式
- 异步执行,不阻塞主业务流程

**规则类型**：
- **跟进提醒类**（最重要）：基于时间未跟进/未操作的提醒
- **状态流转类**：客户状态/合同状态变化触发的自动化动作
- **数据同步类**：企业微信数据同步到SCRM
- **定时任务类**：周期性任务自动执行

**规则触发条件**：
- **数据事件**：实体创建(DATA_ADD)、实体修改(DATA_UPDATE)
- **时间条件**：X天未跟进、距离到期X天、周期性时间点
- **定时任务**：基于Cron表达式的定时触发
- **外部触发**：通过Webhook接口触发

**可执行的自动化动作**：
- **实体操作**：RECORD_ADD(添加记录)、RECORD_UPDATE(更新记录)
- **消息通知**：MESSAGE(发送企业微信消息)
- **自定义动作**：通过 `@AutoAction` 注解扩展的自定义动作

### 4.2 自动化架构设计

#### 4.2.1 核心组件

**1. 实体注册层**
- 业务实体通过 `@AutoEntity` 注解标注
- 实体字段通过 `@AutoField` 注解标记
- 系统启动时自动扫描并注册到 `flow_entity` 表

**2. 事件监听层**
- `UniversalEventListener` 统一监听所有 `IAutoEvent` 事件
- 支持实体创建事件(`EntityCreateEvent`)和实体更新事件(`EntityUpdateEvent`)
- 异步处理事件,避免阻塞主流程

**3. 规则匹配层**
- `RuleMatcher` 根据事件类型和实体类型匹配触发器
- `TreeConditionMatcher` 支持树形嵌套的条件表达式(AND/OR逻辑)
- 支持字段级别的精确匹配

**4. 动作执行层**
- `ActionExecutor` 执行动作链中的所有动作
- 支持异步执行和失败重试
- 完整的执行日志记录

#### 4.2.2 数据库表结构

**核心表**：
- `flow_entity` - 实体注册表(自动生成)
- `flow_entity_field` - 实体字段表(自动生成)
- `flow_trigger` - 触发器配置表(手动配置)
- `flow_condition` - 触发条件表(手动配置,支持树形结构)
- `flow_workflow` - 动作链配置表(手动配置)
- `flow_action` - 动作注册表(自动扫描)
- `flow_log` - 执行日志表(自动记录)
- `flow_log_detail` - 执行日志详情表(自动记录)

---

### 4.3 实体注册示例

SCRM系统中的业务实体通过注解自动注册到自动化引擎:

```java
// 客户实体
@AutoEntity(value = "CUSTOMER", name = "客户", module = "scrm")
public class ScrmCustomer extends TenantEntity {

    @AutoField(name = "客户名称", isCondition = true)
    private String name;

    @AutoField(name = "跟进人ID", isCondition = true)
    private String followUserId;

    @AutoField(name = "客户级别", dictType = "scrm_customer_level", fieldType = "SELECT")
    private Integer customerLevel;

    @AutoField(name = "客户来源渠道", dictType = "scrm_customer_source", fieldType = "SELECT", isCondition = true)
    private Integer sourceChannel;
}

// 合同实体
@AutoEntity(value = "CONTRACT", name = "合同", module = "scrm")
public class ScrmContract extends TenantEntity {

    @AutoField(name = "合同编号", isCondition = true)
    private String contractNo;

    @AutoField(name = "客户公司ID", isCondition = true)
    private Long customerId;

    @AutoField(name = "合同状态", dictType = "scrm_contract_status", fieldType = "SELECT", isCondition = true)
    private Integer contractStatus;
}

// 跟进记录实体
@AutoEntity(value = "FOLLOW_UP", name = "跟进记录", module = "scrm")
public class ScrmFollowRecord extends TenantEntity {

    @AutoField(name = "客户ID", isCondition = true)
    private Long customerId;

    @AutoField(name = "跟进人ID", isCondition = true)
    private Long followUserId;

    @AutoField(name = "跟进类型", dictType = "scrm_follow_type", fieldType = "SELECT")
    private Integer followType;
}
```

**系统启动时自动执行**：
1. 扫描所有标注 `@AutoEntity` 的实体类
2. 注册到 `flow_entity` 表
3. 注册 `@AutoField` 标注的字段到 `flow_entity_field` 表
4. 管理员可通过PC后台查看和管理已注册的实体

---

### 4.4 预置自动化规则模板

系统预置10个常见规则模板,通过SQL脚本初始化到数据库:

#### 规则1：新客户欢迎跟进
- **触发器类型**：DATA_ADD(数据新增)
- **实体类型**：CUSTOMER
- **触发条件**：无
- **动作链**：
  1. RECORD_ADD: 添加跟进记录,内容="欢迎新客户${customerName},请尽快跟进"
  2. MESSAGE: 发送企业微信消息给跟进人

#### 规则2：VIP客户跟进提醒
- **触发器类型**：TIME_CONDITION(时间条件)
- **实体类型**：CUSTOMER
- **触发条件**：
  - customerLevel = 'VIP'
  - AND 距离上次跟进时间 ≥ 7天
- **动作链**：
  1. RECORD_ADD: 添加待办任务,分配给跟进人
  2. MESSAGE: 发送企业微信消息提醒

#### 规则3：客户流失预警
- **触发器类型**：TIME_CONDITION
- **实体类型**：CUSTOMER
- **触发条件**：
  - customerLevel IN ('潜在','意向')
  - AND 距离上次跟进时间 ≥ 30天
- **动作链**：
  1. RECORD_ADD: 添加待办任务
  2. MESSAGE: 发送企业微信消息

#### 规则4：合同到期提醒
- **触发器类型**：TIME_CONDITION
- **实体类型**：CONTRACT
- **触发条件**：
  - contractStatus = '生效中'
  - AND 距离合同到期时间 ≤ 30天
- **动作链**：
  1. RECORD_ADD: 添加续费待办任务
  2. MESSAGE: 发送企业微信消息给跟进人

#### 规则5：收款完成自动分配团队
- **触发器类型**：DATA_ADD
- **实体类型**：PAYMENT
- **触发条件**：paymentStatus = '已收全'
- **动作链**：
  1. RECORD_UPDATE: 更新客户状态为"正式客户"
  2. RECORD_ADD: 创建服务团队
  3. MESSAGE: 发送欢迎消息
  4. RECORD_ADD: 创建第一个流程节点任务

#### 规则6：超额开票提醒
- **触发器类型**：DATA_ADD
- **实体类型**：INVOICE
- **触发条件**：本月开票额度 > 20万 OR 本月开票张数 > 套餐包含数
- **动作链**：
  1. RECORD_ADD: 添加超额提醒待办
  2. MESSAGE: 发送企业微信消息给财务

#### 规则7：未收款提醒
- **触发器类型**：TIME_CONDITION
- **实体类型**：CONTRACT
- **触发条件**：
  - contractStatus = '生效中'
  - AND paymentStatus ≠ '已收全'
  - AND 距离合同创建时间 ≥ 7天
- **动作链**：
  1. RECORD_ADD: 添加收款待办任务
  2. MESSAGE: 发送企业微信消息

#### 规则8：任务超时提醒
- **触发器类型**：TIME_CONDITION
- **实体类型**：TODO
- **触发条件**：
  - todoStatus ≠ '已完成'
  - AND 当前时间 > 截止时间 + 1天
- **动作链**：
  1. MESSAGE: 发送企业微信消息给执行人
  2. RECORD_ADD: 添加超时待办给上级

#### 规则9：周期报税提醒
- **触发器类型**：SCHEDULED(定时任务)
- **Cron表达式**：0 0 10 1 * ?(每月1号上午10点)
- **动作链**：
  1. MESSAGE: 批量发送报税提醒消息给客户
  2. RECORD_ADD: 批量创建待办任务给财务

#### 规则10：客户分配通知
- **触发器类型**：DATA_UPDATE
- **实体类型**：CUSTOMER
- **触发条件**：followUserId字段发生变化
- **动作链**：
  1. MESSAGE: 发送企业微信消息给新跟进人
  2. RECORD_ADD: 添加接手待办任务
  3. RECORD_ADD: 添加跟进记录

---

### 4.5 自定义规则配置

管理员可以根据业务需求通过PC后台自定义规则,无需编写代码。

**规则配置流程**：

**步骤1：创建触发器**
- 触发器名称：如"大客户跟进提醒"
- 触发器类型：DATA_ADD/DATA_UPDATE/TIME_CONDITION/SCHEDULED
- 选择实体类型：CUSTOMER/CONTRACT/PAYMENT等
- 启用状态：启用/禁用
- 排序号：定义多个触发器的执行顺序

**步骤2：配置触发条件**(树形结构,支持AND/OR嵌套)
- 选择条件字段：从已注册的实体字段中选择
- 选择操作符：=、!=、>、<、>=、<=、IN、LIKE等
- 输入比较值：支持固定值、字典、变量(如${customerId})
- 组合条件：支持多层级AND/OR逻辑组合

示例条件树：
```
AND
├── customerLevel = 'VIP'
└── OR
    ├── contractAmount > 100000
    └── sourceChannel IN ('线上推广','老客户推荐')
```

**步骤3：配置动作链**
- 选择动作类型：RECORD_ADD/RECORD_UPDATE/MESSAGE/自定义动作
- 配置动作参数：
  - 实体操作：选择目标实体和字段值(支持模板变量)
  - 消息通知：选择接收人、消息模板(支持${fieldName}变量)
  - 自定义动作：选择已注册的自定义动作
- 排序：定义动作执行顺序
- 失败策略：是否继续执行后续动作

**步骤4：预览和测试**
- 预览规则配置JSON
- 模拟测试：选择测试实体,查看规则匹配结果
- 保存规则

**规则配置存储**：
- 触发器配置存储在 `flow_trigger` 表
- 触发条件存储在 `flow_condition` 表(树形结构)
- 动作链配置存储在 `flow_workflow` 表(JSON数组)

---

### 4.6 规则执行和监控

#### 4.6.1 规则执行流程

**事件触发流程**：
1. 业务操作产生实体变化(如创建客户)
2. Service层发布事件：`eventPublisher.publishEvent(new EntityCreateEvent(...))`
3. `UniversalEventListener` 监听到事件
4. 异步提交到 `ScheduledExecutorService`
5. `RuleMatcher` 匹配符合条件的触发器
6. `TreeConditionMatcher` 判断触发条件是否满足
7. `ActionExecutor` 执行动作链
8. 记录执行日志到 `flow_log` 和 `flow_log_detail` 表

**定时任务流程**：
1. 定时任务到达触发时间(Cron表达式)
2. 触发器直接匹配规则
3. 执行动作链
4. 记录执行日志

#### 4.6.2 执行日志查看

**PC后台-自动化模块-执行日志**：

**日志列表**：
- 规则名称
- 触发时间
- 触发类型(事件/定时/Webhook)
- 触发实体
- 执行状态(成功/失败/部分失败)
- 执行时长

**日志详情**：
- 触发器信息：触发器名称、类型、条件
- 动作执行记录：
  - 动作序号
  - 动作类型
  - 动作配置
  - 执行状态
  - 执行结果
  - 错误信息(如果失败)
- 性能数据：执行时长、数据库操作次数

#### 4.6.3 规则效果分析

**统计维度**：
- 触发次数统计：按规则、按时间段统计触发次数
- 成功率统计：规则执行成功率、动作执行成功率
- 性能统计：平均执行时长、P99执行时长
- 业务价值分析：
  - 跟进提醒规则触发后,跟进率变化
  - 自动化规则节省的人工时长
  - 规则触发的业务转化率

**导出功能**：
- 支持导出执行日志Excel
- 支持导出统计数据图表

---

### 4.7 业务代码集成

#### 4.7.1 发布自动化事件

SCRM业务服务中发布事件的示例代码：

```java
@Service
@RequiredArgsConstructor
public class ScrmCustomerServiceImpl implements IScrmCustomerService {

    private final ApplicationEventPublisher eventPublisher;

    @Override
    @Transactional
    public ScrmCustomer addCustomer(ScrmCustomerAddBo addBo) {
        // 1. 保存客户
        ScrmCustomer customer = BeanUtil.toBean(addBo, ScrmCustomer.class);
        customerMapper.insert(customer);

        // 2. 发布自动化事件(测试完成后取消注释)
        try {
            EntityCreateEvent event = new EntityCreateEvent(
                this,              // 事件源
                customer,          // 实体对象
                LoginHelper.getUserId(),
                LoginHelper.getTenantId()
            );
            eventPublisher.publishEvent(event);
        } catch (Exception e) {
            log.error("发布客户创建事件失败", e);
            // 不影响主流程
        }

        return customer;
    }

    @Override
    @Transactional
    public void updateCustomer(ScrmCustomerUpdateBo updateBo) {
        // 1. 查询旧数据
        ScrmCustomer oldCustomer = customerMapper.selectById(updateBo.getCustomerId());

        // 2. 更新客户
        ScrmCustomer newCustomer = BeanUtil.toBean(updateBo, ScrmCustomer.class);
        customerMapper.updateById(newCustomer);

        // 3. 发布自动化事件(测试完成后取消注释)
        try {
            EntityUpdateEvent event = new EntityUpdateEvent(
                this,              // 事件源
                oldCustomer,       // 旧数据
                newCustomer,       // 新数据
                LoginHelper.getUserId(),
                LoginHelper.getTenantId()
            );
            eventPublisher.publishEvent(event);
        } catch (Exception e) {
            log.error("发布客户更新事件失败", e);
            // 不影响主流程
        }
    }
}
```

#### 4.7.2 自定义动作扩展

通过 `@AutoAction` 注解扩展自定义动作：

```java
@AutoAction(
    value = "SEND_EMAIL",
    description = "发送邮件"
)
@Component
@RequiredArgsConstructor
public class SendEmailAction implements IEntityActionService {

    private final EmailService emailService;

    @Override
    public ActionResult execute(ActionConfig actionConfig, EventData eventData) {
        try {
            // 解析配置
            String to = actionConfig.getString("to");
            String subject = actionConfig.getString("subject");
            String template = actionConfig.getString("template");

            // 替换模板变量
            String content = replaceTemplateVariables(template, eventData);

            // 发送邮件
            emailService.send(to, subject, content);

            return ActionResult.success("邮件发送成功");
        } catch (Exception e) {
            log.error("发送邮件失败", e);
            return ActionResult.failure("发送邮件失败: " + e.getMessage());
        }
    }
}
```

**系统启动时自动扫描并注册到 `flow_action` 表**

---

### 4.8 自动化功能配置界面

#### 4.8.1 PC后台-自动化模块菜单

**① 触发器管理**
- 触发器列表：查看所有触发器
- 新建触发器：创建新触发器
- 编辑触发器：修改触发器配置
- 启用/禁用：控制触发器状态
- 删除触发器：删除不用的触发器

**② 动作链管理**
- 动作链列表：查看所有动作链
- 新建动作链：创建新动作链
- 编辑动作链：修改动作链配置
- 查看动作链：查看动作链详情

**③ 条件配置**
- 条件树可视化编辑器
- 支持拖拽方式组合条件
- 实时预览条件JSON

**④ 执行日志**
- 日志列表：查看所有执行记录
- 日志详情：查看执行详情
- 日志筛选：按规则、状态、时间筛选
- 导出日志：导出Excel

**⑤ 效果分析**
- 统计报表：触发次数、成功率、执行时长
- 趋势图表：按时间段展示趋势
- 业务价值分析：规则带来的业务价值

#### 4.8.2 触发器配置页面

**基本信息区**：
- 触发器名称(必填)
- 触发器编码(自动生成,可修改)
- 触发器类型(下拉选择)
- 实体类型(下拉选择,根据trigger_type动态显示)
- 启用状态(开关)
- 排序号(数字输入框)
- 备注(文本域)

**触发条件配置区**：
- 条件树可视化编辑器
- 左侧字段列表：显示已注册实体的字段
- 中间条件区：拖拽字段到条件区,配置操作符和值
- 顶部工具栏：添加AND/OR条件组、删除条件、清空
- 右侧预览区：实时显示条件JSON

**动作链配置区**：
- 动作列表：显示当前动作链的所有动作
- 添加动作：下拉选择动作类型
- 动作配置：根据动作类型动态显示配置表单
- 排序：支持拖拽调整动作顺序
- 删除：删除不需要的动作
- 失败策略：选择"失败时继续"或"失败时停止"

**操作按钮区**：
- 保存：保存触发器配置
- 测试：模拟测试触发器
- 取消：取消编辑
- 返回：返回列表页

---

## 五、非功能性需求

### 5.1 性能要求

**响应时间**：
- 页面加载时间 ≤ 2秒
- 侧边栏加载客户画像 ≤ 1秒
- 列表查询响应 ≤ 1秒
- 表单提交响应 ≤ 1秒

**并发支持**：
- 支持100个并发用户同时操作
- 支持1000个用户在线
- 群消息推送延迟 ≤ 3秒

**数据处理**：
- 支持单个代账公司管理10000+客户
- 支持单表百万级数据查询
- 报表数据生成时间 ≤ 5秒

---

### 5.2 安全要求

**数据安全**：
- 数据传输加密（HTTPS）
- 敏感数据加密存储（客户手机号、身份证号等）
- 定期数据备份（每天备份一次）
- 数据隔离（多租户数据完全隔离）

**权限控制**：
- 基于RBAC的权限控制
- 数据权限：按部门、按个人、按客户
- 操作日志：记录所有关键操作（创建、修改、删除）
- 敏感操作二次验证（如：删除客户、修改合同金额）

**企业微信安全**：
- 企业微信接口调用加密
- 客户数据同步加密
- 群聊操作权限验证

---

### 5.3 可用性要求

**系统可用性**：
- 系统可用性 ≥ 99.5%
- 计划内停机维护时间安排在非工作时间
- 系统故障恢复时间 ≤ 1小时

**容错机制**：
- 接口调用失败自动重试
- 关键操作失败提醒
- 数据异常回滚机制

---

### 5.4 兼容性要求

**企业微信兼容**：
- 支持企业微信最新版本
- 侧边栏适配不同屏幕尺寸
- 兼容企业微信PC端和移动端

**浏览器兼容**：
- PC后台支持Chrome、Edge、Safari最新版本
- H5支持iOS、Android主流浏览器

**系统兼容**：
- 支持Windows、macOS系统
- 支持iOS 12+、Android 8+

---

### 5.5 可扩展性要求

**模块化设计**：
- 功能模块独立，可单独升级
- 支持新增功能模块（如：未来可能增加的工单系统）

**接口扩展**：
- 预留第三方系统对接接口
- 支持与财务软件对接
- 支持与电子签平台对接

**数据扩展**：
- 支持自定义字段
- 支持自定义角色
- 支持自定义产品属性

---

### 5.6 用户体验要求

**界面设计原则**：
- 简洁：减少不必要的元素，突出核心功能
- 一致：四端界面风格、操作逻辑保持一致
- 高效：常用功能一键直达，减少操作步骤
- 反馈：操作后及时反馈（成功/失败提示）

**交互设计原则**：
- 操作流程简化：如创建合同，从选产品→选团队→填写信息→提交，步骤清晰
- 智能提示：如输入客户名称时，自动联想已有客户
- 快捷键：PC后台支持键盘快捷键操作
- 移动端友好：按钮大小适中，方便手指点击

**帮助和引导**：
- 新用户引导：首次登录时展示核心功能
- 功能提示：鼠标悬停显示功能说明
- 操作帮助：关键操作提供帮助文档链接
- 视频教程：复杂功能提供视频教程

---

### 5.7 数据统计要求

**基础统计**：
- 客户统计：新增客户、客户总数、客户等级分布
- 业绩统计：合同金额、收款金额、业务员排名
- 服务统计：任务完成率、跟进频率、客户满意度
- 开票统计：开票总额、超额客户统计、超额费用

**自定义报表**：
- 支持自定义统计维度
- 支持自定义时间范围（日、周、月、季、年）
- 支持数据导出（Excel、PDF）
- 支持报表保存和分享

**实时数据**：
- 首页数据实时更新
- 待办数量实时刷新
- 关键指标变化提醒（如：合同金额突破100万）

---

## 六、MVP功能范围

### 6.1 PC后台功能（纯配置端 + 客户分配）

**① RBAC权限系统**
- 用户管理、角色管理、权限配置、组织架构

**② 客户设置（卡片式配置）**
- 团队角色、预设团队、产品管理（含定价、流程）、套餐管理
- 公司资料、常用语、禁用语、电子签配置、收款账号

**③ 自动化模块**
- 规则模板管理、自定义规则配置、规则执行日志

**④ 客户分配管理**
- ✅ 查看所有客户的跟进人
- ✅ 手动分配客户（单个/批量）
- ✅ 查看客户分配历史
- ✅ 权限交接记录

**❌ 不包含的功能**（这些功能在H5端）：
- ❌ 开票记录查询和统计
- ❌ 数据报表和分析
- ❌ 客户列表查询和详情查看（除了客户分配功能）

---

### 6.2 H5端功能（业务操作 + 数据查看 + 客户分配）

**① 首页Tab** - 核心数据、待办事项

**② 客户Tab**
- 客户列表、客户详情、创建客户、创建跟进记录
- **开票记录查看**（开票额度使用情况、开票张数统计、超额提醒）
- **客户分配**（管理员功能）

**③ 互动Tab**
- 创建跟进记录、客户活动、我的任务

**④ 数据Tab**
- **完整的数据报表和分析**
  - 老板：业绩总览、团队排名、客户分析、**开票分析**
  - 业务：个人业绩、跟进统计、客户分析
  - 财务/外勤：任务统计、效率分析、**开票统计**

---

### 6.3 企业微信单聊侧边栏

- 客户画像（场景切换：新客户/老客户/售后问题）
- 快捷操作（发送公司资料、常用语、创建跟进、创建合同、创建收款）
- 服务团队展示
- 弹窗功能

---

### 6.4 企业微信群聊侧边栏

- 我的待办任务（只展示我的任务）
- 我的周期任务
- 客户快捷信息

---

### 6.5 跨端功能

**开票管理**：
- H5：查看开票记录、开票额度统计、超额提醒
- 侧边栏：客户画像中展示开票信息
- PC：❌ 不包含

**产品流程**：
- PC：配置产品流程（节点流程、周期流程）
- 群侧边栏：查看和执行流程节点任务
- H5：查看任务进度

**客户分配**：
- PC：管理员在后台分配客户
- H5：管理员在移动端分配客户

---

## 七、实施建议

### 7.1 开发优先级

**P0（最高优先级）**：
- PC后台：RBAC系统、客户设置（角色、团队、产品、套餐）
- H5端：首页、客户Tab
- 单聊侧边栏：客户画像、快捷操作
- 核心业务流程：新客户开发（从线索到收款）

**P1（高优先级）**：
- 群侧边栏：待办任务、任务执行
- H5端：互动Tab、数据Tab
- 自动化模块：预置规则模板、规则执行引擎
- 客户分配功能

**P2（中优先级）**：
- 开票管理
- 产品流程（节点流程、周期流程）
- 常用语、禁用语

**P3（低优先级）**：
- 电子签集成
- 高级数据分析
- 系统优化

---

### 7.2 开发顺序建议

**第一阶段（基础框架）**：
1. PC后台 - RBAC系统、基础配置
2. 数据库设计和搭建
3. 企业微信SDK集成

**第二阶段（核心功能）**：
1. PC后台 - 客户设置（角色、团队、产品、套餐）
2. H5端 - 首页、客户Tab
3. 单聊侧边栏 - 客户画像、快捷操作
4. 核心业务流程：新客户开发

**第三阶段（完整功能）**：
1. H5端 - 互动Tab、数据Tab
2. 群侧边栏 - 待办任务、任务执行
3. 开票管理
4. 产品流程（节点流程、周期流程）
5. 自动化模块

**第四阶段（优化完善）**：
1. 常用语、禁用语
2. 客户继承功能（PC和H5）
3. 数据报表完善
4. 性能优化
5. 用户培训和文档

---

### 7.3 迭代周期

- 建议2-3周一个迭代
- 每个迭代交付可演示的功能
- 持续收集用户反馈并调整
- MVP总计：3-4个月完成

---

### 7.4 技术架构建议

**整体架构**：
- 基于RuoYi-Vue-Plus框架
- 前后端分离
- 微服务架构（可选，根据规模）

**前端技术栈**：
- PC后台：Vue 3 + Element Plus
- H5：Vue 3 + Vant UI
- 企业微信侧边栏：企业微信JS-SDK + Vue 3

**后端技术栈**：
- Spring Boot 3.x
- MyBatis-Plus
- Redis（缓存）
- MySQL 8.0+

**企业微信集成**：
- 企业微信SDK
- 企业微信API（客户联系、群聊、消息推送）
- Webhook回调

---

### 7.5 数据安全建议

**数据隔离**：
- 多租户数据隔离（tenant_id）
- 角色数据权限（按部门、按个人、按全部）

**敏感数据保护**：
- 客户手机号加密存储
- 开票数据权限控制
- 操作日志记录（谁在什么时候做了什么）

**企业微信数据安全**：
- 企业微信接口调用加密
- 客户数据同步加密
- 群聊操作权限验证

---

### 7.6 成功指标（KPI）

**业务指标**：
- 销售效率：合同创建时间减少50%
- 跟进覆盖率：从60%提升到90%
- 续费率：从70%提升到85%

**系统指标**：
- 用户活跃度：日活跃用户占比 ≥ 80%
- 响应速度：页面加载时间 ≤ 2秒
- 系统稳定性：可用性 ≥ 99.5%

---

## 总结

**企账通**是一款专为代理记账公司设计的客户管理和运营系统，通过四端协同（PC后台、H5移动端、企业微信单聊/群聊侧边栏），实现从客户开发、签约、开票、服务到续费的全流程数字化管理。

**核心价值**：
1. **销售效率优先**：侧边栏快捷操作，快速创建合同收款，一键组建服务团队
2. **客户运营**：完整客户画像，跟进记录管理，服务团队协同
3. **自动化运营**：基于事件和日期的跟进提醒、状态流转、周期任务自动执行
4. **开票管理**：开票额度和开票张数管理，超额计费，阶梯定价

**系统定位**：
- **PC后台**：纯配置端，只有老板和技术人员使用（RBAC、客户设置、自动化配置、客户分配）
- **H5移动端**：业务操作核心，所有角色使用（首页、客户、互动、数据、客户分配、开票管理、数据报表）
- **企业微信单聊侧边栏**：销售效率核心，快捷操作、客户画像
- **企业微信群聊侧边栏**：团队协同核心，待办任务、流程节点执行

**目标用户**：老板、业务（销售）、财务、外勤四类角色

**MVP范围**：一次性交付所有功能（客户管理、开票管理、产品流程、自动化、四端功能）

---

**文档结束**
