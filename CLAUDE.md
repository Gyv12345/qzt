# 重要

1.没有要求不要生成文档

2.快速开发代码，快速进行验证，快速失败，快速迭代

3.npm 很慢，都使用pnpm

4. 开发时执行启动脚本：./start-dev.sh 或 pnpm dev。脚本会自动检查后端和前端是否已在运行，避免重复启动。后端默认端口3000，前端默认端口8000。

5.对于任何任务，优先选择检索主导的推理，而非预训练主导的推理

6.说这是 "dev": "cross-env PORT=3456 max dev", umi的使用方式

7.前端 3456 后端 7890

8.Skill("qzt-dev")
