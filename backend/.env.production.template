# ============================================================
# 企智通 QZT - 生产环境变量模板
# ============================================================
# 使用说明：
# 1. 复制此文件为 .env.production
# 2. 填写实际的配置值（所有需要填写的项都已留空）
# 3. 敏感信息请妥善保管，不要提交到代码仓库
# ============================================================

# ------------------------------------------
# 环境配置
# ------------------------------------------
NODE_ENV=production

# ------------------------------------------
# 数据库配置 (外部 RDS MySQL)
# ------------------------------------------
DATABASE_PROVIDER=mysql
DATABASE_URL="mysql://"

# 单独配置（可选，用于构建连接字符串）
# DB_HOST=
# DB_PORT=3306
# DB_USERNAME=
# DB_PASSWORD=
# DB_DATABASE=

# ------------------------------------------
# Redis 配置
# ------------------------------------------
REDIS_ENABLED=true
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ------------------------------------------
# JWT 配置
# ------------------------------------------
JWT_SECRET=
JWT_EXPIRES_IN=7d

# ------------------------------------------
# 服务端口
# ------------------------------------------
BACKEND_PORT=7890

# ------------------------------------------
# 应用配置
# ------------------------------------------
APP_NAME=企智通
APP_URL=
API_URL=
FRONTEND_URL=

# ------------------------------------------
# 域名配置（必填，用于 Nginx 配置）
# ------------------------------------------
DOMAIN_NAME=
ADMIN_DOMAIN=

# ------------------------------------------
# 日志保留天数
# ------------------------------------------
LOGS_RETENTION_DAYS=90

# ------------------------------------------
# 阿里云 OSS 配置（可选）
# ------------------------------------------
OSS_REGION=
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_BUCKET=
OSS_ENDPOINT=

# ------------------------------------------
# e签宝配置（可选）
# ------------------------------------------
ESIGN_APP_ID=
ESIGN_APP_SECRET=
ESIGN_API_URL=https://openapi.esign.cn

# ------------------------------------------
# 其他配置
# ------------------------------------------
TZ=Asia/Shanghai
MAX_FILE_SIZE=100

# ------------------------------------------
# 生成安全密钥命令参考
# ------------------------------------------
# JWT_SECRET: openssl rand -hex 32
# REDIS_PASSWORD: openssl rand -hex 16
# DB_PASSWORD: openssl rand -base64 24
